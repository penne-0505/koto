# 日本語プログラミング言語　設計メモ

## 1. プロジェクトの動機と目的

### 主目的
日本語ドリブンでプログラムを書くと、思考フレームがどう変わるかを観察する。

### 従目的
簡単なAPIが書ける程度の実用性を持たせる。「これですべてを書く」言語にはしない。

### 動機の背景
- 英語を知らないとプログラムが理解できないことへの違和感
- キーワードだけでなく、識別子（変数名・関数名）まで英語になることへの不満
- 言語は思考のフレームワークに強く作用するという信念（Sapir-Whorf的な視点）
- 語彙置換ではなく、日本語の文法をなるべく保った形にしたい

### なでしことの差異
なでしこは「日本語で書ける実用言語」が主目的。思考フレームへの作用は意図された主題ではない。
この言語は逆の重み付けで、思考への作用が主、実用性が従。

### 観察の手段
当面は自分で書いて内省する。他人に書かせる場合はドキュメントや学習教材が必要になるが、現時点では自己観察が基本。

---

## 2. 評価基準

~~「散文的であること」~~

→ **「日本語話者が読めて、かつ日本語の文法的特性が活きている構文か」**

散文であることが基準ではなく、以下の条件を満たすかどうかで構文を評価する。

- 日本語話者にとって読める
- 助詞が意味を運んでいる
- 語順自由性がある
- 日本語の構造を活かしている

---

## 3. 確定した設計方針

### 3-1. 語順と語彙

| 項目 | 方針 |
|------|------|
| 語順 | 自由。助詞が意味役割を運ぶので、語順を入れ替えても同じ意味として受理する |
| 語彙 | 固定。`数値配列` を `数値リスト` と書き換えることはできない。`繰り返す` を `ループする` と書けないのと同じ |
| 分かち書き | 採用。全角スペースで単語を区切る |
| 数値 | 算用数字に統一。漢数字は使わない（熟語の構成要素は除く） |
| インデント | 見やすさのため。全角スペースで階層を表現。なくても構文的には成立する |

### 3-2. 宣言の構文

宣言の種類を `〜として` で統一的に明示する。

```
関数　として　〜
オブジェクト　として　〜
定数　として　〜
可変　として　〜
```

### 3-3. 型の表現

- 型注釈は `〜である〜` で挟む：`数値 である 現在の最大`
- TypeScript的な型推論：推論可能なら省略できる、推論できない場面では必須
- 関数シグネチャの型（引数型・戻り値型）は常に明示
- Optional型は「たぶん〜」で表現する可能性あり（未確定だが有力候補）

### 3-4. 集まり系の語彙

型名は複合名詞で書く。

| 概念 | 書き方 | 空のもの |
|------|--------|----------|
| 配列 | `数値配列`、`タスク配列` | `空配列` |
| 集合 | `数値集合`、`タスク集合` | `空集合` |
| 辞書 | `文字列とタスクの辞書` | `空辞書` |
| 組 | 保留（知名度低、PoCでは不要かもしれない） | — |

辞書は二項を取るため、複合名詞ではなく `〜と〜の辞書` で表現する。

### 3-5. インデックス

1起点。`1番目の要素`、`2番目の要素`、`最後の要素`、`最初の要素`。

`〜番目の要素` の前は算用数字に限定。`一番目の要素` は不可。

### 3-6. ミュータビリティ

- デフォルトは不変
- 可変にしたい場合は `可変として` を明示
- `定数として` は明示してもいいが、普通は不要（デフォルトが不変なので）

```
タスク配列　である　結果　を　空配列　とする。               ← 不変（デフォルト）
可変として　タスク配列　である　結果　を　空配列　とする。    ← 可変
定数として　タスク配列　である　結果　を　空配列　とする。    ← 不変（明示、省略可能）
```

### 3-7. 等価判定

三層を書き分ける。

| 構文 | 意味 | 英語圏での対応 |
|------|------|----------------|
| `〜と同じ` | 値として等しい | `==` |
| `〜そのもの` | 同一インスタンス | `is` |
| `〜と同種` | 同一型 | `type(a) == type(b)` |

### 3-8. 関数呼び出し

全角丸括弧 `（）` で引数を囲む。複数引数は読点で区切る。

```
最大値（対象ら）
未完了の取り出し（タスクら）
何かの処理（引数1、引数2、引数3）
現在の日時（）          ← 引数なしでも括弧は必須
```

### 3-9. 制御フロー

**return：** `〜を返す。` で関数を完了する。「返す」は完了感を内包する。

**throw：** `〜を例外として終える。` で処理を打ち切る。throw のメタファーから離れ、「例外という様態で関数を終える」という意味。return とは動詞を変えて、終了経路の質的な違いを表現する。

**条件分岐：** `もし 〜 なら、〜。` で表現。if-else は強制しない。

```
もし　今の数　が　現在の最大　より大きいなら、
　　現在の最大　を　今の数　とする。
```

**繰り返し：** `要素を 〜 として 〜 の 各要素について繰り返すと、〜 である。`

```
要素を　今の数　として　対象ら　の　各要素について繰り返すと、
　　（本体）
である。
```

### 3-10. 記号の用途

| 記号 | 用途 |
|------|------|
| `「」` | 文字列リテラル |
| `（）` | 関数呼び出し・コンストラクタ呼び出しの引数 |
| 句点 `。` | 文の終端。ブロックを閉じる役割も兼ねる |
| 読点 `、` | ブロックの開始、列挙の区切り |
| 全角スペース | 分かち書き |

識別子は裸で書く（括弧なし）。

### 3-11. 型定義

**オブジェクト：**
```
オブジェクト　として　タスク　は、
　　文字列　である　タイトル、
　　タスク状態　である　状態、
　　日付　である　締め切り、
　　からなる。
```

**列挙型：**
```
タスク状態　は、未完了、進行中、完了　のいずれか。
```

列挙値の参照時、フィールドの型から推論できる場合は型名を省略可能。
`今のタスク の 状態 が 未完了 と同じなら` と書ける（`タスク状態の未完了` と書かなくてよい）。

**インスタンス生成：**
```
タスク（
　　タイトル　が　「買い物」、
　　状態　が　未完了、
　　締め切り　が　今日。
）　を　新しいタスク　とする。
```

フィールドの順序は自由（助詞でフィールド名を指定しているため）。
型が自明な場合は型注釈を省略可能。

**メソッド（将来追加）：**
```
関数　として　タスク　の　完了する　は、
　　自身　の　状態　を　完了　とする。
```

型定義の外部に書く形式（Rust の impl、Go のメソッド定義に近い）。`自身` が self/this に相当。

### 3-13. モジュールシステム

**ファイル＝モジュール。** 一つのファイルが一つのモジュールに対応する。

**取り込み（import）：**
```
タスク定義　から　タスク　を　取り込む。
タスク定義　から　タスク、タスク状態　を　取り込む。
```

**可視性：** デフォルト非公開。公開したいものは `公開` を宣言の頭に付ける。

```
公開　関数　として　数値配列　である　対象ら　を受け取る　数値　を　返す　最大値　は、
公開　オブジェクト　として　タスク　は、
```

`公開` は分かち書きで前置する（複合名詞にしない）。修飾語が複数ある場合に積み重ねられるようにするため。

```
公開　可変として　タスク配列　である　結果　を　空配列　とする。
```

**モジュールの参照方法：** パスリテラル。鉤括弧 `「」` の中にファイルパスを書く。

```
「タスク定義」から　タスク　を　取り込む。                        ← 同じ階層
「モデル/タスク定義」から　タスク、タスク状態　を　取り込む。    ← 下位階層
「../ユーティリティ/日付変換」から　日付変換　を　取り込む。      ← 相対参照
```

書いたパスとファイルシステム上の実体が一致する。拡張子は省略。

### 3-14. トランスパイル先とFFI

**トランスパイル先：JavaScript（Node.js）。**

あなたの言語のソースコードをJavaScriptファイルに変換する。変換されたコードはJavaScriptそのものなので、JavaScriptの全ライブラリが使える。将来フロントエンドに進む道も開ける。

**外部ライブラリの呼び出し：宣言ファイル方式（TypeScriptの `.d.ts` に相当）。**

外部JavaScriptライブラリを使うとき、あなたの言語の構文で「外の世界にこれがある」と宣言する。利用者はJavaScriptを直接書く必要がない。コード生成器が宣言を見て適切なJavaScriptを出力する。

```
外部　オブジェクト　として　クライアント　は、
　　文字列　である　トークン　を受け取る　何も返さない　ログイン、
　　からなる。
```

宣言ファイル自体もあなたの言語の構文で書く。型チェックが効く。

**標準ライブラリ：** JavaScriptの既存ライブラリの日本語ラッパーとして段階的に構築する。APIデザイン（関数名、助詞パターン、型の使い方）はこの言語の設計原則に従い、利用者はJavaScriptの概念を意識しない。

### 3-15. PoCの目標とスコープ

**最初の目標：Discord botが書けること。**

API呼び出し、非同期イベント処理、JSON、文字列操作が含まれる現実的な題材。

**PoCの段階：**

- 第一段階：パーサ＋型チェッカー＋中間表現まで（言語仕様を固める）
- 第二段階：中間表現→JavaScript生成（Discord botが動く）

**将来の方向：**

TypeScriptクラスの自由度を持つ言語として、バックエンドもフロントエンドも書けることを目指す。ただしPoCでは範囲を絞る。

### 3-16. プロパティアクセスと添字アクセス

いずれも `〜の〜` で書く。

```
対象ら　の　1番目の要素        ← 配列の添字アクセス
対象ら　の　長さ                ← プロパティアクセス
対象ら　の　最後の要素          ← プロパティアクセス
タスク表　の　「タイトル1」    ← 辞書のキーアクセス
今のタスク　の　状態            ← オブジェクトのフィールドアクセス
```

添字とキーの区別は型情報で解決。構文上は区別しない。

### 3-17. 非同期処理

**非同期関数の宣言：** `非同期` を修飾語として前置する。

```
非同期　関数　として　メッセージ　を受け取る　何も返さない　メッセージ処理　は、
　　もし　メッセージ　の　内容　が　「!ping」　と同じなら、
　　　　メッセージ　に　「pong」　を返信して待つ。
```

`公開 非同期 関数 として` のように修飾語を積める。

**await：** 日本語のて形接続を使う。`〜して待つ`。

```
メッセージ　に　「pong」　を返信して待つ。
データ　を　取得して待つ。
```

「返信する」＋「待つ」をて形で繋ぐ。日本語として自然に読める。

### 3-18. 高階関数とmap/filter

**関数の受け渡し：** 名前付き関数の名前をそのまま値として渡す。特別な構文不要。

```
クライアント　の　イベント登録（「メッセージ受信」、メッセージ処理）。
ボタン　の　クリック時に（処理）。
```

イベント登録は言語構文ではなくメソッド呼び出し。ライブラリのAPIデザインの問題。

**匿名関数：PoCでは不要。** 名前付き関数の受け渡しで対応する。「匿名関数がないとき、書き手は何に名前を付けることになるか」は思考フレームへの作用の観察対象。必要と感じたら後から追加。

**map/filter：標準ライブラリとして実装。** 言語構文ではなく配列オブジェクトのメソッド。宣言的な書き方を採用。

```
対象ら　の　各要素　の　タイトル　を　集める。          ← map相当
対象ら　の　うち、状態　が　未完了　と同じ　であるもの。  ← filter相当
```

---

## 4. 例文集

### 4-1. 最大値（基本的な関数、ループ、条件分岐）

```
関数　として　数値配列　である　対象ら　を受け取る　数値　を　返す　最大値　は、
　　もし　対象ら　が　無し　と同じなら、
　　　　「対象らが無し。」を例外として終える。
　　対象ら　の　1番目の要素　を　数値　である　現在の最大　とする。
　　要素を　今の数　として　対象ら　の　各要素について繰り返すと、
　　　　もし　今の数　が　現在の最大　より大きいなら、
　　　　　　現在の最大　を　今の数　とする。
　　である。
　現在の最大　を　返す。
```

### 4-2. 未完了の取り出し（フィルタリング、オブジェクト操作）

```
関数　として　タスク配列　である　対象ら　を受け取る　タスク配列　を　返す　未完了の取り出し　は、
　　可変として　タスク配列　である　結果　を　空配列　とする。
　　要素を　今のタスク　として　対象ら　の　各要素について繰り返すと、
　　　　もし　今のタスク　の　状態　が　未完了　と同じなら、
　　　　　　結果　に　今のタスク　を　加える。
　　である。
　結果　を　返す。
```

### 4-3. タスク表の作成（辞書操作）

```
関数　として　タスク配列　である　対象ら　を受け取る　文字列とタスクの辞書　を　返す　タスク表の作成　は、
　　可変として　文字列とタスクの辞書　である　結果　を　空辞書　とする。
　　要素を　今のタスク　として　対象ら　の　各要素について繰り返すと、
　　　　結果　に　今のタスク　の　タイトル　と　今のタスク　を　加える。
　　である。
　結果　を　返す。
```

### 4-4. 型定義

```
オブジェクト　として　タスク　は、
　　文字列　である　タイトル、
　　タスク状態　である　状態、
　　日付　である　締め切り、
　　からなる。

タスク状態　は、未完了、進行中、完了　のいずれか。
```

### 4-5. インスタンス生成

```
タスク（
　　タイトル　が　「買い物」、
　　状態　が　未完了、
　　締め切り　が　今日。
）　を　新しいタスク　とする。
```

### 4-6. 宣言的な書き方（参考・未確定）

```
関数　として　タスク配列　である　対象ら　を受け取る　タスク配列　を　返す　未完了の取り出し　は、
　　対象ら　の　うち、状態　が　未完了　と同じ　であるもの　を　返す。
```

---

## 5. 未決定事項

### 優先度高（次に議論すべき）

| # | 項目 | 備考 |
|---|------|------|
| 1 | エコシステムの範囲 | ~~方向性は決定済み~~。標準ライブラリはJSラッパー、外部ライブラリは宣言ファイル方式。パッケージ配布方法は未定 |
| 2 | メソッド構文の詳細 | 方向性は決まっているが、具体的な構文は未確定 |
| 3 | 高階関数 | ~~基本方針は決定済み~~。名前付き関数の受け渡し。匿名関数はPoCでは不要 |
| 4 | エラーハンドリングの詳細 | try-catch に相当する構造。例外を投げる構文は決まったが、受ける側が未定 |

### 優先度中

| # | 項目 | 備考 |
|---|------|------|
| 5 | 受動態の扱い | 「保存する」と「保存される」を区別するか。主題（「は」）に意味を持たせるか |
| 6 | 主語省略の扱い | 日本語は主語を省く。パイプライン的に直前の結果を暗黙参照するか |
| 7 | 宣言的な書き方の仕様化 | 「〜のうち、〜であるもの」「〜の各要素の〜を集める」。標準ライブラリのAPIとして実装する方針だが、構文の詳細は未定 |
| 8 | ユーザー定義型のジェネリクス | 辞書は `〜と〜の辞書` で解決したが、ユーザー定義の型コンストラクタは未定 |
| 9 | Optional型の構文確定 | 「たぶん〜」を正式採用するか |
| 10 | ローマ字記述の許可 | 漢字との曖昧性をどう扱うか。観察対象としての価値 |

### 開発ツール・エコシステム

| # | 項目 | 優先度 | 備考 |
|---|------|--------|------|
| 11 | TextMate Grammar（シンタックスハイライト） | **最優先・次のステップ** | 正規表現ベース。キーワード・記号・文字列リテラルの色分け。言語設計と並行して作れる。この言語はキーワードと識別子が同じ文字体系なので、ハイライトの必要性が英語ベース言語より高い |
| 12 | LSP：診断（エラー表示） | 高 | パーサのエラー出力をそのまま返す。パーサ完成後に載せる |
| 13 | LSP：セマンティックハイライト | 高 | 型名・変数名・キーワードを意味に基づいて色分け。この言語で特に価値が高い |
| 14 | LSP：自動補完・ホバー情報 | 中 | 型チェッカー完成後に載せる |
| 15 | LSP：定義ジャンプ・参照検索・リネーム | 中 | 名前解決完成後 |
| 16 | LSP：シグネチャヘルプ・コードフォーマット | 低 | あると嬉しいが後回し |
| 17 | モジュールシステム | ~~決定済み~~ | ファイル＝モジュール、パスリテラル参照、デフォルト非公開 |
| 18 | パッケージ・パッケージマネージャ | 中 | npm上に載せるか、独自に持つか。宣言ファイルの配布方法 |
| 19 | テストの書き方 | 中 | アサーション構文、テストブロックの構文。LLM時代にテストを快適に書けない言語は使いたくない（ユーザー所感） |
| 20 | デバッガ（DAP） | 低 | PoCでは不要 |

### 言語設計の残り

| # | 項目 | 優先度 | 備考 |
|---|------|--------|------|
| 21 | 非同期処理 | ~~基本方針は決定済み~~ | `非同期 関数 として`、`〜して待つ`（て形接続） |
| 22 | パターンマッチ | 低 | 「確認する：」ブロックの発展形 |
| 23 | 敬語の扱い | 低 | プログラム内で意味を持つか、捨てるか |
| 24 | トランスパイラの実装言語・方針 | 中 | パーサ＋型チェッカー＋コード生成器をどの言語で書くか |
| 25 | 宣言ファイルの構文詳細 | 中 | 外部JavaScriptライブラリの型宣言の書き方 |

---

## 6. 設計上の原則（暗黙に合意されたもの）

- 「日本語プログラミング言語」ではなく「日本語の特性を活かしたプログラミング言語」
- 慣習に従う動機がない。慣習から外れることで「制御フローの言語化」が進む場面がある
- PoCではIR（中間表現）までを第一段階とし、JavaScript生成を第二段階とする
- 「なぜ作るか」は思考フレームへの作用の観察。実用ツールとしての生産性は二の次
- 英語の概念世界は輸入品であることを認める。日本語化の意味は「概念を日本由来にする」ことではなく「助詞・語順・修飾の自由度を日本語話者にとって自然にする」こと
- この言語にとってのエコシステムは「観察に十分な厚みのあるプログラムを書くために最低限何が要るか」から逆算する。一般的な言語の「ユーザーを集めて育てる」エコシステム論とは異なる
- JavaScriptにトランスパイルする。外部ライブラリは宣言ファイル方式で型付きで呼ぶ。標準ライブラリはJavaScriptライブラリの日本語ラッパーとして段階的に構築し、利用者はJavaScriptの概念を意識しない

---

## 7. 次のステップ

### 最優先：TextMate Grammar の作成

VSCode拡張としてシンタックスハイライトを実装する。言語仕様が確定していなくても、キーワードの色分けだけで書き味の印象が大きく変わる。

**キーワード一覧（ハイライト対象）：**

宣言系：`関数`、`オブジェクト`、`定数`、`可変`、`外部`、`公開`、`非同期`
制御系：`もし`、`なら`、`繰り返すと`、`である`、`からなる`、`のいずれか`
動作系：`返す`、`として終える`、`して待つ`、`取り込む`、`とする`、`を受け取る`、`を返す`
構文系：`として`、`である`、`の`、`が`、`に`、`を`、`から`、`で`、`と同じ`、`そのもの`、`と同種`

**記号：**

- `「」` — 文字列リテラル
- `（）` — 関数呼び出し・コンストラクタ
- `。` — 文の終端
- `、` — ブロック開始・列挙区切り

**リテラル：**

- 算用数字（`0-9`）
- `真`、`偽`
- `無し`

**ファイル拡張子：** 未定（`.jp`、`.nihon`、`.koto`、`.nh` など候補）

### その後

1. 実際にこの言語でDiscord botのコードを書いてみる（手書き、パーサなし）
2. 書いた結果を見て、構文の調整・追加の判断点を洗い出す
3. パーサの実装に着手
